import 'package:flutter/material.dart';

class PessoaTile extends StatelessWidget {
  const PessoaTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
